# Claude Code session data (settings, worktrees, memory)
.claude/
.claire/

# Editor / OS
*.swp
*.swo
*~
.DS_Store

# Build artifacts / scratch
*.log
*.tmp
.cache/

# Local state (never commit chroots/VMs/manifests)
/var-lab-create/
/state/

# Downloaded artifacts (fetched per INSTALL-KALI-KEYRING.md, not sources)
phase1-chroot/kali-archive-keyring_*.deb

# Phase 6 — Python tooling artifacts
phase6-tui/.venv/
phase6-tui/.pytest_cache/
phase6-tui/**/__pycache__/
phase6-tui/**/*.pyc
phase6-tui/*.egg-info/

# Micro-Linux lab — build outputs + fetched/extracted sources.
# (versions.lock and keys/ live outside out/ and stay tracked.)
micro-linux/out/
__pycache__/
