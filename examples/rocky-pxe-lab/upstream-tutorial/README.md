# Upstream tutorial — archived copy

An **unmodified, byte-exact archive** of the knowledge-base article that this lab
operationalizes, vendored here for offline reference and provenance.

| | |
|---|---|
| **Title** | *PXE Boot Installation of Rocky Linux Using Lorax, dnsmasq, and TFTP* |
| **Author** | CIQ Knowledge Base (CIQ, Inc.) |
| **Canonical URL** | <https://kb.ciq.com/article/rocky-linux/rl-booting-rocky-linux-via-pxe> |
| **KB home** | <https://kb.ciq.com/> |
| **Retrieved** | 2026-06-07 |

> **Title note:** the lab's README cites this as *"Booting Rocky Linux via PXE"*
> (matching the URL slug); the article's full on-page title is the longer one
> above. Same article.

## Files

| File | sha256 |
|---|---|
| [`rl-booting-rocky-linux-via-pxe.html`](rl-booting-rocky-linux-via-pxe.html) | `a0c822e165a600e5227426a2d1f7a19eb703243009bfb0a54864b764c459ebc5` |
| [`style.css`](style.css) | `ebb45f440e9ddc252c7aa7259ec3ca371ca52ae4b8b7400c31d8c8942bd61e0d` |

The HTML is saved exactly as served. This is a **Next.js** site, so the full
article body is present twice in the file — once as rendered markup and once as
JSON inside the page's embedded `__NEXT_DATA__` — which means the text is fully
readable offline even without JavaScript. The page's compiled stylesheet
(`/_next/static/chunks/…css`) is vendored as `style.css`; the web fonts
(`api.fontshare.com`) and the JS bundles are **left as-is** (absolute links to
the live site). Because the live page hydrates with JavaScript, an offline open
renders as unstyled-but-complete article text rather than pixel-perfect.

## Copyright & attribution

This article is the work of **CIQ, Inc.**, and **all rights and copyright remain
with them.** It is archived here solely as an offline, fixed-point reference for
the [`../`](../) lab, which implements its `dnsmasq` ProxyDHCP + TFTP PXE recipe
(Path B) under QEMU and on real hardware. For the authoritative, maintained
version — and to support the authors — always go to the [canonical
page](https://kb.ciq.com/article/rocky-linux/rl-booting-rocky-linux-via-pxe). If
you are a rights-holder and would prefer this copy not be redistributed, removing
it is a one-line `git rm`.
