# Runtime state: TLS material issued from the shared CA, registry storage, and the
# client --cert-dir. All of it is regenerated by `registry-lab.sh certs` + `up`, and the
# TLS KEY must never be committed (examples/lab-ca keeps the same split).
state/
