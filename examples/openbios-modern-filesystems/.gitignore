# The fs corpus is regenerated by fixtures/fs-corpus/build-fs-corpus.sh (and by
# smoke-fs-tiers.sh into a temp dir) — the images are not source, only the builder
# and the derived fs-tiers.toml are committed.
fixtures/fs-corpus/out/
