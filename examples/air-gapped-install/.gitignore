# All of it is regenerated by `airgap.sh mirror` + `install`: the partial mirror
# tree, the throwaway signing key (a PRIVATE key must never be committed), the GnuPG
# home, the bootstrapped rootfs and the control logs.
state/
